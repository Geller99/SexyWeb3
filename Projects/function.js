

/*
    Get numbers return an array of numbers that are less than or equal to the target number

    grab all numbers that are greater than or equal to  

*/

const example = [0, 1, 5, 6, 7, 4, 3, 9, 2];

const grabNumbers = (arr, targetNumber) => {

    // can I ask if I can mutate this array ?
    // who's respomsible for this memory ?
    // is it important to think about negatives ?
    // time complexity ? big O notation Space complexity ?
    //  

}